<!DOCTYPE html>
<html lang="en">
<%- include('./includes/head.ejs') %>

    <body>
        <%- include('./includes/header.ejs') %>
        <%- include('./includes/menu.ejs') %>
        <%- include('./includes/loading.ejs') %>
        <div id="channel-container">
            <div class="channel-list">
                <div id="box-search">
                    <select id="category" name="category">
                        <option disabled selected hidden>카테고리를 선택하세요.</option>
                        <option value="개발">개발</option>
                        <option value="디자인">디자인</option>
                        <option value="영업">영업</option>
                        <option value="커뮤니티">커뮤니티</option>
                        <option value="교육">교육</option>
                        <option value="기타">기타</option>
                    </select>
                    <input type="search" id="search" placeholder="검색">
                    <button id="btn__search"><i class="fa-solid fa-magnifying-glass fa-lg"></i></button>
                </div>
                <div id="tab-box">
                    <a class="type" href="#">전체</a>
                    <a class="type" href="#">개발</a>
                    <a class="type" href="#">디자인</a>
                    <a class="type" href="#">영업</a>
                    <a class="type" href="#">커뮤니티</a>
                    <a class="type" href="#">교육</a>
                    <a class="type" href="#">기타</a>
                </div>
                <div class="wrapper-title-channel-container">
                    <div id="title">
                        🥰 그이상의 소통 | 공개 채널
                    </div>
                    <div class="channel-box-items">
                        <%if(channelList.length > 0) { %>
                            <% for(let channel of channelList) { %>
                                <div class="contents__div-wrapper" onclick="onClickChannelBox('<%= channel._id %>')">
                                    <img src="<%= channel.thumbnail %>">
                                    <div class="card">
                                        <h2>채널명 | <%= channel.channelName %></h2>
                                    </div>
                                    <div class="card">
                                        <% for(let category of channel.category) { %>
                                            <h5 href="/client/mychannels/<%= channel._id %>"> 카테고리 | <a class="hash" href="#">#<%= channel.category %></a></h5>
                                        <% } %>
                                    </div>
                                    <div class="card">
                                        <button id="<%= channel._id %>" class="heart" type="button" onclick="onClickHeartToggleBtn('<%= channel._id %>')">
                                            <% if(channel.isUserWishChannel) { %>
                                                <i class="fa-solid fa-heart fa-xl"></i>
                                            <% } else { %>
                                                <i class="fa-regular fa-heart fa-xl"></i>
                                            <% } %>
                                        </button>
                                    </div>
                                </div>
                            <% } %>
                        <% } else if(!channelList){ %>
                            <div id="channel-container">
                                <div class="channel-list">
                                    <div id="error">
                                        <img src="/images/icons8-spinner.gif" alt="loading">
                                        <h1>서버와 연결이 끊겼습니다.</h1>
                                    </div>
                                </div>
                            </div>
                        <% }else { %>
                            <div id="channel-container">
                                <div class="channel-list">
                                    <h1>현재 개설된 채널이 없습니다.</h1>
                                </div>
                            </div>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>	
        <%- include('./includes/script.ejs') %>
    </body>
</html>