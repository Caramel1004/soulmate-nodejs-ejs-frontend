<!DOCTYPE html>
<html lang="en">
    <%- include('../includes/head.ejs') %>
    <body>
        <%- include('../includes/header.ejs') %>
        <%- include('../includes/menu.ejs') %>
        <div class="channel-container">
            <div class="channel-list">
                <div id="box-search">
                    <select id="category" name="category">
                        <option disabled selected hidden>카테고리를 선택하세요.</option>
                        <% for(let category of staticCategoryList) { %>
                            <option value="<%= category.keyword %>"><%= category.name %></option>
                        <% } %>
                    </select>
                    <input type="search" id="search" placeholder="원하는 채널을 검색하세요.">
                    <button id="btn__search"><i class="fa-solid fa-magnifying-glass fa-lg"></i></button>
                </div>
                <div id="tab-box">
                    <a class="type" id="all" href="/mychannels?searchWord=all">전체</a>
                    <a class="type" id="own" href="/mychannels?searchWord=own">내가 만든채널</a>
                    <a class="type" id="invite" href="/mychannels?searchWord=invite">소속 채널</a>
                    <a class="type" id="open" href="/mychannels?searchWord=open">오픈 채널</a>
                    <a class="type" id="work" href="/mychannels?searchWord=work">워크스페이스</a>
                </div>
                <div class="wrapper-title-channel-container">
                    <div id="title">
                        <div>🥰 <span class="client-name"><%= clientName %></span>님의 채널 | 그이상의 소통을 만들어 보세요</div>
                    </div>
                    <div class="channel-box-items">
                        <%if(channelList.length > 0) { %>
                            <% for(let channel of channelList) { %>
                                <div class="contents__div-wrapper">
                                    <img src="<%= channel.thumbnail %>">
                                    <div class="channel-name-card">
                                        <span><%= channel.channelName %></span>
                                    </div>
                                    <div class="card">
                                        <i class="fa-solid fa-user"></i><span><%= channel.members.length %></span>
                                    </div>
                                    <div class="card">
                                        <% for(let category of channel.category) { %>
                                            <a class="hash" href="#">#<%= channel.category %></a>
                                        <% } %>
                                    </div>
                                    <div class="card">
                                        <a class="a__btn-enter" href="/mychannel/<%= channel._id %>?searchType=info">채널 입장</a>
                                    </div>
                                </div>
                            <% } %>
                        <% } else { %>
                            <h1 style="text-align: center;">나만의 채널을 추가 해보세요!!</h1>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>		
        <%- include('../includes/script.ejs') %>
    </body>
</html>